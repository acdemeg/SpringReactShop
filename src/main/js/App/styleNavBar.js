const navBarStyle = {
  backgroundColor: 'Gainsboro',
  height: '50px',
  display: 'flex',
  alignItems: 'center',
  borderRadius: '0px',
};

const rightPartNavBar = {
  position: 'absolute',
  right: '1%',
};

const style = {
  navBarStyle,
  rightPartNavBar,
};

export default style;
